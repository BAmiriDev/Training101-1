package Week4.Mon

class FibonacciCalc {



  object FibonacciCalc extends App {
    def fibonacci(x: Int): Either[String, Int] = {
      def helper(num: Int, acc: Int): Either[String, Int] = {

      }


    }

  }
    //  if (seq.length >= seqSize) {
    //    seq
    //  } else {
    //    Fibonacci(seq :+ (seq(seq.length - 1) + seq(seq.length - 2)))
    //  }
  }
